%div#main

  %header
    %h1= link_to "บ้านฉันจมหรือยัง", "/"

  %section#landing
    = form_tag new_report_path, method: :post, id: 'initiate_report' do
      %h2.description กรอกรหัสไปรษณีย์ของพื้นที่ที่ท่านพักอาศัยอยู่
      %p.field<
        = text_field_tag :zone_id, "", size: 20, class: 'postcode', maxlength: 5, autofocus: true, pattern: '[0-9]*'
        = submit_tag "แจ้งสถานการณ์"

    = form_for @report, method: :post do |f|
      %h2
        แจ้งสถานการณ์ในพื้นที่
        %span#zone_id_label
        = f.hidden_field :zone_id

      %p.buttonset
        = f.radio_button :flooded, true
        = f.label :flooded_true, "จมแล้ว"
        = f.radio_button :flooded, false
        = f.label :flooded_false, "ยังไม่จม"

      %p
        = f.text_field :comment
        = f.text_field :author

      %p
        = f.submit "บันทึก"

%div#sub

  %header
    %h2 สถานการณ์ล่าสุด

= render @zones
